The solution was created to monitor MS SQL updates against DB Tabple that contains Shopping Cart records.

1. Backbone app that refreshes html content after ajax comet request returns update signal: new-data
~\Views\ShoppingCart\Monitor.cshtml

2. Backbone app (little solution) that shows items from the Invoices table.
~\Views\InvoiceManager\Index.cshtml

3. jQuery exammple.
Shows images from the backend (but it was not completed)
~\Views\ShoppingCart\Index.cshtml

4. jQuery plugin grid table (small sample)
~\Views\Designs\Index.cshtml
